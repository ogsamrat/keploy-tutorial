# Tutorial screenshots

These are the real terminal screenshots used throughout the tutorial, numbered in the order they
appear:

- `01-record-pull.png` through `10-test-failing.png`

To swap any of them for your own, keep the same filename and drop the new image in this folder.
If a file is ever missing, the page shows a tidy placeholder in its place, so the layout never
breaks.
